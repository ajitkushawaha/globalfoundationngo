# Core
MONGODB_URI=your_mongodb_connection_string
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-strong-secret

# Option A: Gmail SMTP (recommended for simplicity)
# Enable 2-Step Verification on your Google account and create an App Password here:
# https://myaccount.google.com/apppasswords
# Then set:
# EMAIL_USER=yourgmail@gmail.com
# EMAIL_PASS=your_16_char_app_password
# Optional custom From header (defaults to EMAIL_USER if omitted)
# EMAIL_FROM="GEKCT Foundation" <yourgmail@gmail.com>

# Option B: Custom SMTP server
# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=user@yourdomain.com
# SMTP_PASS=your_smtp_password
# Optional custom From header
# SMTP_FROM="GEKCT Foundation" <no-reply@yourdomain.com>

# Optional: Protect test email endpoint
# TEST_EMAIL_TOKEN=some-long-random-string
